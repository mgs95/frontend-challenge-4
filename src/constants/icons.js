/** CHALLENGE NOTE
 *
 * It is a good idea to keep all icon names in the same place so they can be easily found
 * and updated. It will also make it easier to change the icons font (if required).
 */

export const sendMessage = 'fa-paper-plane';
